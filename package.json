{
    "name": "file-util",
    "module": "index.ts",
    "type": "module",
    "version": "0.0.1",
    "bin": {
        "cpjs": "index.ts"
    },
    "scripts": {
        "build_exe": "bun build ./index.ts --outfile dist/cpjs --compile",
        "build": "bun build --outdir dist --target bun index.ts"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "bun": "^1.1.18"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}